import "./App.css"
import { Button } from "@heroui/react";

export const App = () => (
  <div className="App">
    <header className="App-header">
      <Button color="primary" >Button</Button>
    </header>
  </div>
)
